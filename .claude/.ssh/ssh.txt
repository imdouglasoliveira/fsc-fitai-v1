# 🧪 Testar Conexões SSH
ssh -T git@github-imdouglas
ssh -T git@github-gabriel
ssh -T git@github-fappssh
ssh -T git@github-moradigna
ssh -T git@github-adriano
ssh -T git@github-neolingo

# ❌ ERRO COMUM: Não use git@github.com
# ✅ SEMPRE use o alias: git@github-adriano

# 📥 Clonar (use o ALIAS!)
git clone git@github-adriano:adrianomorais-ganbatte/repo.git

# 🔍 Diagnóstico rápido
ssh-add -l                                    # Ver chaves carregadas
cat ~/.ssh/config | grep -A 5 "github-adriano"  # Ver configuração
git remote -v                                  # Ver remote do repo